{
  "name": "8fulfill",
  "version": "1.0.0",
  "private": true,
  "description": "Static browser app for generating BBDG assessment ZIP files.",
  "scripts": {
    "start": "serve .",
    "check": "node --check app.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@tesseract.js-data/eng": "^1.0.0",
    "serve": "^14.2.5",
    "tesseract.js": "^7.0.0"
  }
}
